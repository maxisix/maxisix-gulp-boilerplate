/* global describe, it, afterEach */
'use strict';

delete require.cache[require.resolve('..')];
var watch = require('..'),
    should = require('should');

describe('calculateBase', function () {
    it('should do something', function () {
        console.log(watch.calculateBase(['test/fixtures/**/*.scss'], {
            cwd: process.cwd(),
            path: ''
        }));
    });
});
